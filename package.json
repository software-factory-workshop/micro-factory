{
  "name": "micro-factory",
  "private": true,
  "packageManager": "pnpm@10.33.4",
  "engines": {
    "node": ">=24.11.0 <25"
  },
  "scripts": {
    "dev": "pnpm --filter @micro-factory/factory dev",
    "build": "pnpm --filter @micro-factory/factory build",
    "typecheck": "pnpm --filter @micro-factory/factory typecheck",
    "test": "pnpm --filter @micro-factory/factory test",
    "check": "pnpm typecheck && pnpm test && pnpm build"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
