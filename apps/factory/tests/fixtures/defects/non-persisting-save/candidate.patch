diff --git a/app/components/TodoList.vue b/app/components/TodoList.vue
index 052d02b..b63e422 100644
--- a/app/components/TodoList.vue
+++ b/app/components/TodoList.vue
@@ -36,8 +36,9 @@ async function complete(id: string) {
   if (busy.value) return;
   busy.value = true;
   try {
-    await $fetch(`/api/todos/${id}`, { method: "PATCH" });
-    await refresh();
+    // Optimistic completion: flip the row locally so the list feels instant.
+    const todo = items.value.find(item => item.id === id);
+    if (todo) todo.completed = true;
   } finally {
     busy.value = false;
   }
