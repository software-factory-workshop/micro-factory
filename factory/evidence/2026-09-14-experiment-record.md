# Experiment record · deleted test caught by a host rule, not by a reviewer's eye

Recorded 14 September 2026. One factory change, measured with the table from `workshop/factory-growth-experiment.md`. This is an observation on fixtures and unit tests, not statistical proof; the paid eval against a live PR is listed under Replay as pending.

| Field | Record |
| --- | --- |
| Request and expectation | Input: the `[injected]` `deleted-test` candidate (`apps/factory/tests/fixtures/defects/deleted-test/candidate.patch`) removes the unit test covering todo completion; every remaining check passes. Expected before the run: the quality gate records `changes_requested` with a blocking finding, whatever the model writes. |
| Versions | Factory revision `5ec441c` (micro-factory `main`, 14 Sep 2026). Target base: `adeo-todo-nuxt` `ed34fe6` (shell, 2 Vitest tests). Fixture version: first. Model: `meta/muse-spark-1.3-contributor` for every station; settings in `apps/factory/runtime/lib/factory-config.ts`. |
| Before | In the Jira factory the reviewer approved PR #2 (12 Sep 2026) after moving its missing checks from limitations to "optional"; required evidence lived in model wording. A deleted test produced green exit codes and no host signal. Artifact: `jira-clone` review of PR #2 and the host-owned review-policy fix in PR #5. |
| Change | `verify_review` parses the unit test total on the pristine base and on the head (`testCountFromOutput`, node --test and Vitest summary lines), and records a blocking `VerificationFinding` when head < base before `record_review` runs. `hostReviewLimitations` adds "unit test count on head is below base" to the required evidence, so `approve` throws at the host. Files: `runtime/lib/command-evidence.ts`, `runtime/lib/review-policy.ts`, `runtime/stations/gate/tools/verify_review.ts`, `runtime/stations/gate/tools/record_review.ts`. Reason: the signal is deterministic and cheaper than a model judgement, and it cannot be argued away in prose. |
| Replay | Live level, 14 Sep 2026 18:40: in reference run attempt 4 (delivery `798479e8`) the quality gate's `verify_review` parsed both totals (base 2, head 19) and approval was allowed; in attempt 2 the same host rule refused approval when the base total was unparseable, which proves the rule blocks on missing evidence rather than defaulting to approve. Unit level, 14 Sep 2026: `tests/gate-host-rules.test.ts` "the deleted-test fixture is caught deterministically by the test-count delta" passes (base 9, head 8 → one blocker "below base"). Live level: pending. Run `FACTORY_RUN_INJECTED_DEFECT_EVAL=1 FACTORY_INJECTED_DEFECT_FIXTURE=deleted-test FACTORY_INJECTED_DEFECT_PR=<pr> pnpm exec eve eval injected-defect-review --url <cockpit-url> --strict` once a person has opened the fixture PR in `adeo-todo-nuxt` and the cockpit is deployed with the GitHub connector attached. |
| Transfer | Related case not used to tune the fix: a test skipped with `it.skip` instead of deleted. Vitest reports `Tests 8 passed \| 1 skipped (9)`; the parser reads the total in parentheses (9), so the count does not drop and the delta does not fire. The skipped test is caught only if the gate reads the output (limitation recorded here, not hidden). Second related case: a test file renamed and its cases halved; the delta fires. |
| Human effort | One coding session (Claude Fable 5.1 driven by Remi's kickoff brief), about 20 minutes on this change including the Vitest summary parser and its tests. No manual intervention in the product. |
| Cost and limits | Model usage for this change: not measured (development session, not a factory run). Factory run receipts will carry `usd`, `inputTokens`, `outputTokens` when the provider reports them. Elapsed: the unit suite runs in under 2 s. Stop reason: unit replay complete; live replay blocked on connector coverage and a fixture PR (see friction log). |
| Conclusion | Improved at unit level; inconclusive live until the paid eval runs. The `it.skip` transfer case is a known gap to add as a second host rule (skipped-count delta) in M2. |
