# Evidence

Dated records of what the factory actually did. Excluded from every agent snapshot (`includeSource` in `apps/factory/runtime/lib/github.mjs`), so nothing here becomes prompt context.

- `2026-09-14-reference-run.md`: state of the reference run and the command sheet to finish it.
- `2026-09-14-experiment-record.md`: the experiment-record table for one factory change (test-count delta).
- `2026-09-14-friction-log.md`: summary of the `frog` entries under `.agents/friction-log/`.
